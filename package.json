{
  "name": "harness50-validation",
  "version": "2.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test codex/tests/*.test.mjs",
    "test:browser": "npm --prefix browser-verifier test",
    "verify:output": "node scripts/verify-output.mjs"
  },
  "devDependencies": {
    "axe-core": "4.13.0"
  }
}
