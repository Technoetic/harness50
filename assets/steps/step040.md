---
name: step040
persistence: session
---

# Step 40 - 조사 스크린샷 vs 구현 스크린샷 비교 검증 (독립 검증 루프)

<!-- MOAI-ENRICHED v1 -->
> **📐 Plan → Run → Sync** (MoAI-ADK 워크플로우)
> - **Plan**: 본 Step의 SPEC 자동 생성 `step_archive/specs/SPEC-040.md` 를 먼저 읽고 Acceptance 기준을 확정한다.
> - **Run**: 본문 지침대로 실행. 구현 산출물에는 `@MX:NOTE` 최소 1개 부착 (위험 시 `@MX:WARN` + `@MX:REASON`, 계약 시 `@MX:ANCHOR` + `@MX:REASON`, 미완료 시 `@MX:TODO`). MoAI mx-tag-protocol SoT 준수.
> - **Sync**: 결과 파일 `step_archive/step040_*.md` 저장 후 1줄 완료 보고 `Step 040/50 완료`.
>
> **모델 정책**: 조사·구현 서브에이전트 = **haiku** (CLAUDE.md 정책 준수). 평가 라운드만 sonnet.
>
> **위치**: 구현·정리 구간 (E2E 검증 step045 전)

## 실행 내용

Step 39 레이아웃 검증 통과 후, **조사 단계에서 촬영한 참고 스크린샷**과 **현재 구현 스크린샷**을 나란히 비교하여 디자인 품질을 검증한다.

**필요한 파일:**

- `step_archive/screenshots/research/awwwards-*.png` (Step 22 Awwwards 레이아웃 조사 스크린샷 — 비교 기준)
- step030_레이아웃설계_chunk*.md (레이아웃 설계)

**이 단계에서 절대로 superpowers:brainstorming을 사용하지 않는다.**

**스크린샷 없이 통과 처리 금지. 반드시 Claude가 직접 스크린샷을 눈으로 확인한다.**

## 최종 조사 기준

`step_archive/outputs/step024_검증_r1.md`의 최종 PASS manifest와 통합 분석을 먼저 읽는다.
비교 기준은 기존 참고 이미지와 최종 manifest에서 채택한 보충 이미지·조사 축을 모두 포함한다.
검증자는 보충 원문·스크린샷도 직접 열어 확인한다. 기존 glob에 없는 보충 이미지를 누락하거나
보충 전에 부족하다고 판정된 Step 23 결론만으로 비교 기준을 되돌리지 않는다.

## QA 완료 증거 (필수)

신뢰한 설치 플러그인의 `docs/QA-REPORTS.md`와 `scripts/qa-report.mjs`를 사용한다.
각 시도 시작에 `inspect --workspace "<project-root>" --step 40`으로 이전 실패를 확인한다.
수정과 필요한 build를 마친 뒤 검증 전에 `snapshot`을 만든다. 실제 소스·설정·검증 대상
산출물과 본문의 모든 필수 검사(화면·viewport·상태 조합 포함)를 명시한다.
검증자의 실제 관찰과 스크린샷·실행 결과를 `record`로 기록한 뒤 다시 `inspect`한다.
`status=current`와 `verdict=PASS`를 모두 확인해야 완료 보고 및 다음 Step 진입이 가능하다.
필수 실패·증거 누락·미실행·stale은 INCOMPLETE다. 수정 뒤에는 새 snapshot과 재검증이 필요하다.
검증 전 snapshot을 검증 후 새로 만들어 과거 결과를 현재 PASS로 바꾸지 않는다.

## 핵심 원칙: 작성 에이전트 ≠ 검증 에이전트

- **에이전트 B (검증 전문)**: Sonnet - 스크린샷 비교, 문제 판정, 합격/불합격 결정만 담당. **코드 수정 금지**
- **에이전트 A (수정 전문)**: Sonnet - 에이전트 B의 판정에 따라 코드만 수정. **합격/불합격 판정 금지**

이 분리를 통해 검증자의 편견 없는 판정과 수정자의 빠른 피드백 루프를 보장한다.

---

## 검증 절차

### 1단계: Awwwards 기준 스크린샷 수집

1. `step_archive/outputs/step024_검증_r1.md`의 최종 PASS manifest에서 채택한 기존·보충 원문과 이미지의 정확한 경로·SHA-256·통합 조사 축으로 비교 목록을 만든다. 기존 glob만으로 목록을 제한하지 않는다.
2. 경로를 해석하고 hash를 대조한 뒤 원문을 읽고 각 이미지를 직접 열어 수상작·화면·조사 축을 확인한다. 최종 PASS 누락, 입력 누락·hash 불일치 또는 필수 이미지 미검사는 INCOMPLETE다.

### 2단계: 구현 스크린샷 촬영

1. 개발 서버를 실행한다.
2. Playwright로 주요 뷰포트별(데스크톱, 태블릿, 모바일) 스크린샷을 촬영한다.
3. `step_archive/screenshots/compare-impl-desktop.png`, `compare-impl-tablet.png`, `compare-impl-mobile.png` 로 저장한다.

### 3단계: 비교 검증 루프

#### 3-1. 에이전트 B의 역할 (검증)

**에이전트 B에게 최종 Step 24 보고서와 manifest에서 만든 전체 비교 목록을 전달한다. B는 보고서를 직접 읽고 통합 분석·조사 축 및 모든 기존·보충 입력의 경로·hash를 대조한 뒤 각 채택 이미지에 대해 다음을 반복한다:**

1. Awwwards 스크린샷을 Read한다.
2. 구현 스크린샷을 Read한다.
3. 다음 축으로 비교하여 **PASS/FAIL 판정만 수행한다**:
   - **레이아웃 구조**: 전체 배치, 영역 분할, 비율이 Awwwards 수상작의 우수한 패턴을 반영했는가
   - **시각적 계층**: 정보 계층(제목, 본문, 부가정보)이 명확히 구분되는가
   - **여백/간격**: 요소 간 여백이 Awwwards 수준으로 충분하고 균일한가
   - **색상/대비**: 가독성과 심미성이 Awwwards 수준에 도달했는가
   - **컴포넌트 완성도**: 버튼, 카드, 패널 등 개별 요소가 Awwwards 수준으로 다듬어졌는가
   - **컨트롤 패널 배치**: Awwwards 사례에서 파악한 최적 위치에 배치되었는가
   - **시각화 영역 비율**: 메인 콘텐츠 영역이 충분한 공간을 확보했는가
   - **반응형 전환**: 뷰포트별 레이아웃 전환이 Awwwards 수상작처럼 자연스러운가

4. **판정 결과:**
   - 실제 검사한 기존·보충 입력의 경로·SHA-256, 조사 축과 구현 화면의 대응을 QA 증거에 기록한다.
   - **PASS**: 모든 축이 Awwwards 수준 달성 → 해당 항목 종료
   - **FAIL**: 부족한 부분을 구체적으로 기록하여 에이전트 A에 전달

#### 3-2. 에이전트 A의 역할 (수정)

1. 에이전트 B의 FAIL 판정과 구체적 지적사항을 받는다.
2. **코드만 수정한다** (HTML/CSS/JS):
   - 에이전트 B가 지적한 부분에 집중
   - Awwwards 수상작의 우수 패턴 반영
3. 스크린샷 재촬영
4. 재촬영한 스크린샷을 에이전트 B에 전달

#### 3-3. 반복 제한: 최대 5라운드

최대 5라운드까지 수정·재검증한다. 같은 필수 항목이 3연속 미수정이면 조기 종료한다.
모든 필수 항목의 현재 증거가 PASS일 때만 완료한다. 실패·누락·미검증 또는 한도 소진이면
미해결 항목과 다음 검사를 기록하고 현재 Step을 INCOMPLETE로 인계한다.
필수 실패를 스킵하거나 완료 보고 후 다음 Step으로 진행하지 않는다.

## 통과 조건

- 모든 비교 축에서 Awwwards 수상작 수준의 품질에 도달
- 레이아웃 구조가 Awwwards 사례의 우수 패턴을 반영
- 수정→재촬영→재비교 루프가 더 이상 문제를 발견하지 않음

합리적인 선에서 최대한 많은 서브에이전트를 병렬로 사용한다 (동시 실행 최대 10개).

- **에이전트 B (검증)**: sonnet 사용 - 스크린샷 비교·분석 담당
- **에이전트 A (수정)**: sonnet 사용 - 코드 수정 담당

---

필수 요구와 현재 검증 증거가 모두 PASS일 때만 이 지침을 완료하고 자동으로 step041.md를 읽고 수행한다. 사용자 확인을 기다리지 않는다.

